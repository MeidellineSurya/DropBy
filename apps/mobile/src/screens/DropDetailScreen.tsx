import React from "react";
import { Text, View } from "react-native";

// TODO: renders progressively per stage (detect/reveal/discover) —
// see app/services/proximity.py for what each stage exposes.
export function DropDetailScreen() {
  return (
    <View>
      <Text>Drop detail</Text>
    </View>
  );
}
