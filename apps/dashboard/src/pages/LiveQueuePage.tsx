// TODO: live checked-in queue for this business, subscribed to
// ws:business:{business_id} redemption.checked_in events, with a Confirm
// action per row -> POST /redemptions/{id}/confirm.
export function LiveQueuePage() {
  return (
    <div>
      <h1>Live redemption queue</h1>
    </div>
  );
}
