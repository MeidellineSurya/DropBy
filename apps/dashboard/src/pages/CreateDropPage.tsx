// TODO: form for offer, min/max group size, duration, discovery radius,
// rarity, scheduling -> POST /business/drops.
export function CreateDropPage() {
  return (
    <div>
      <h1>Create a Drop</h1>
    </div>
  );
}
